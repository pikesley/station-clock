--- d3clock/d3clock.js	2021-01-17 14:57:52.000000000 +0000
+++ d3clock/d3clock-hacked.js	2021-01-17 15:13:38.000000000 +0000
@@ -20,8 +20,9 @@
   formatSecond = d3.time.format("%S");
   formatMinute = d3.time.format("%M");
   formatHour = d3.time.format("%H");
-  width = config.width ? config.width : 1000;
-  height = width/2;
+  width = config.width ? config.width : 720;
+  height = width;
+  rasp_width = 150
 
   var outerRadius = 0.8 * height/2;
   var offSetX = height/2;
@@ -133,7 +134,7 @@
           },
           clockHandFill: function(d){
             if (d.unit==="seconds"){
-              return "#e00";
+              return "#c41949";
             } else {
               return "#333";
             }
@@ -161,7 +162,7 @@
             .attr('cy', function(d, i) {
               return -outerRadius + that.tickUnit*3*3 + that.tickUnit*4
             })
-            .attr("fill", "#e00");
+            .attr("fill", "#c41949");
 
           },
           clockGroupAdditional: function(clockGroup) {
@@ -413,6 +414,15 @@
   //create the basic visualization:
   vis = d3.select(config.target).append("svg:svg").attr("width", width).attr("height", height).attr("class","clock");
 
+  // hacked in by me, I remember very little about how D3 works
+  raspberry = vis.append("svg:image")
+                  .attr("xlink:href", "assets/raspberry.svg")
+                  .attr("width", function() { return rasp_width })
+                  .attr("x", function() { return (width / 2) - (rasp_width / 2); })
+                  .attr("y", function() { return (height / 2) + ((height / 6) - (rasp_width / 2)); })
+  raspberry.style("text-anchor", "middle")
+
+
   clockGroup = vis.append("svg:g").attr("transform", "translate(" + offSetX + "," + offSetY + ")");
 
   //create the outer circle of the clock face
